import { Time } from '@angular/common';

export class Registration {
  _id: string;
  bibNumber: number;
  gender: string;
  firstName: string;
  lastName: string;
  birthDate: string;
  created_at: string;
  timingResult: string;
  newBibNumber: number;
}
